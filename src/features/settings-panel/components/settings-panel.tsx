import { Panel } from "@shared/components/panel";

export const SettingsPanel = () => {
  return (
    <Panel position="right" width="md" collapsible>
      <Panel.Title>Settings Panel</Panel.Title>
    </Panel>
  );
};
